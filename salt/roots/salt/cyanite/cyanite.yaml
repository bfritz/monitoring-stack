logging:
  level: info
  console: true
  files:
    - "/var/log/cyanite/cyanite.log"
input:
  - type: "carbon"
store:
  cluster: "localhost"
  keyspace: "metric"
index:
  type: memory
api:
  port: 8080
engine:
  rules:
    default:
      - "5s:1h"
